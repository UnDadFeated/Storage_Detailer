# CHANGELOG.md

<!-- ANCHOR -->
## [1.2.0] - 2026-03-19
### Added
- Instant Snapshot Export (📷 button) for cropped hardware stat images.
- Automated base-10 capacity string injection for precise Amazon eCommerce link generation.
- Extensive SMART Drive Health monitoring and NVMe Wear percentage trackers.
- Fallback CSS selector `result__body` for resilient DuckDuckGo Web intelligence scraping.
- `os.makedirs` path generation for robust UI screenshot directory handling.

### Fixed
- Fixed critical execution exit logic for `smartctl` returning positive health threshold bitmasks natively evaluating as fatal.
- Strip out unreliable Amazon bot/captcha scraper in favor of clean referral link integration.
- Fixed UI layout regressions, enforcing strict 24x60px bounds on actionable affiliate buttons.
- Addressed hanging thread locks implicitly occurring when piping silent `pkexec`/`sudo` routines lacking `-n` parameter bindings.

## [1.1.0] - 2026-03-19
### Added
- SMART native `sudo` and `pkexec` privilege escalations with runtime tracking.
- Implemented robust regex filtering evaluating standard system block names globally shielding against malformed I/O path injection queries safely.

### Fixed
- Redesigned thread instantiation dropping UI hanging during concurrent `subprocess` loads.
- Fixed `QProgressBar` style margin offsets resolving text vertical alignments universally.

## [1.0.0] - 2026-03-19
### Added
- Initial project creation: Storage Detailer.
- Modern Google antigravity GUI style applied across standard fixed viewport layout (680x500).
- Deep `lsblk` metrics layout processing Drive Hardware metadata exclusively (Size, Model, Serial, Firmware, etc.).
- Dropdown storage node selection capturing natively both core NVMe/SATA lines and external USB drives immediately.
